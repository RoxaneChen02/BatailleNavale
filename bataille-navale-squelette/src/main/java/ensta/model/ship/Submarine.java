package ensta.model.ship;
import ensta.util.Orientation;

public class Submarine extends AbstractShip {
    public Submarine(String ship_label, String ship_name, int ship_length, Orientation o ){super(ship_label,ship_name, ship_length, o);}

    public Submarine(){super();}
}
